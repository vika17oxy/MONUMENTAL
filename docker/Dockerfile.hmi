FROM node:22-alpine

RUN corepack enable && corepack prepare pnpm@10.33.0 --activate

WORKDIR /app

COPY vika-hmi/package.json ./
RUN pnpm install --prefer-offline || pnpm install

COPY vika-hmi/ .

EXPOSE 5173

CMD ["pnpm", "dev", "--host", "0.0.0.0"]
